// 1. Encontrar primos gemelos 


const primosGemelos = (limit) => {
    // La funcion primero de bebe encontrar si un numero es primo y luego encontrar todos los pares de numeros primos gemelos sin pasarce del limite(limit)
    // TU CODIGO AQUI:
    
    return limit
}

// 2. Finanzas: Calcular el interes compuesto

 const calcularInteresCompuesto = (inversion, tasa, años) => {
    // La funcion recibe una inversion ( String . Ej: '300 USD' , ' 134,5 $'  etc)
    // Una tasa anual ( String. Ej: '40 %' , '15 %' etc) y los años.
    // La funcion debe calcular la tasa de interes compuesto, segun la la inversion,
    // la tasa y los años y retornarlo en formato string acompañado de la moneda en 
    // que se inviertio. Ej: " 233 $" 
    // TU CODIGO AQUI:

 }

// 2. Calculo de media aritmetica

const calularMediaAritmetica = (numbers) => {
    // La funcion recibe un array de numeros, se debe calcular la media aritmetica
    // de essos numeros y devolver la media
    // TU CODIGO AQUI:

    return numbers
}

// 3. Multiplicacion de Matrices

const matricesProduct = (matriz1, matriz2) => {
    // La funcion recibe 2 matrices (arrays) , a su vez cada matriz 2 arrays (EJ: [[], []])
    // La funcion de cumplir con lo siguiente:
    // - Verificar si se pueden multiplicar las matrices - 
    // - Si no se pueden multiplicar retornar el mensaje "Matrices incompatibles"
    // - En caso de que se pueda, retornar una nueva matriz con nuevos resultados 
    // - Si ambas matrices son unidimensionales (tienen una sola fila o una sola columna)
    // - Debe retornar el mensaje, "Son Matrices unidimensionales"
    // (EJ - Entrada: matriz1 = [[3, 2, 5], [3, 1, 5]]. matriz2 = [[23, 2, 9], []])
    // TIP : Recorda que la regla en algebra lineal es que para que las 
}   

// 4. Calcular Determinante de Matrices 2x2

const determinatMatrizTwoByTwo = (matriz) => {
    return matriz
}

// 5. Calcular Determinate de Matrices 3x3

const determinatMatrizThreeByThree = (matriz) => {

// 6. Suma de vectores en 2 dimensiones

const vectorsSumTwoDimensions = () => {

}



}

module.exports = {
    primosGemelos,
    calcularInteresCompuesto,
    matricesProduct,
    determinatMatrizTwoByTwo,
    determinatMatrizThreeByThree
}
